# OS cruft
.DS_Store
Thumbs.db

# Visual Studio / C++ build junk
*.obj
*.pdb
*.ncb
*.suo
*.user
*.aps
*.sdf
*.opensdf
ipch/
*.log

# NOTE: not ignoring Debug/Release dirs by name — bin/debug and bin/release
# here hold checked-in installer assets (.ini/.bat), not compiler output.

# Eclipse CDT
.metadata/

# Jekyll build output
docs/_site/

# Local mirror of the Google Code Archive packages (see README, Phase 0) —
# ~1.2 GB of installers/SDKs, to be published as a GitHub Release, not committed.
archive-mirror/
