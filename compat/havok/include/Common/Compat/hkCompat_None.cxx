// Thin forwarding stub (Havok->Bullet compat shim, Phase A).
// Real Havok/hkCompat_None.cxx wires up binary-compatibility shims for
// loading older-version .hkx packfiles. This shim does not parse .hkx at
// all yet (see hkpHavokSnapshot::load, Phase B), so there is nothing to
// wire up -- empty on purpose.
