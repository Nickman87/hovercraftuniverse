// Thin forwarding header (Havok->Bullet compat shim, Phase A).
// Real path: Physics/Dynamics/Collide/hkpCollisionListener.h (mirrored so the game's #include <Physics/Dynamics/Collide/hkpCollisionListener.h> resolves unchanged).
// Actual implementation lives in havok_compat/HavokAll.h -- see
// docs/porting/havok-compat.md.
#pragma once
#include "havok_compat/HavokAll.h"
